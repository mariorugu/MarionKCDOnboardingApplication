using System;

namespace DAL.Core
{
    public enum Gender
    {
        None,
        Female,
        Male
    }
    
    public enum Role
    {
        User,
        Employee,
        Manager
    }
}
